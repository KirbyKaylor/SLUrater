<% include chunks/head %>
<% include chunks/nav %>
<div class="category">
    <h1>Dining Services</h1>
    
    <table>
        <tr>
            <th>Name</th>
            <th>Description</th>
        </tr>
        <% diningPage.forEach(function(diningPage) { %>
            <tr>
                <td><a href="/diningpage/<%= diningPage._id %>"><%= diningPage.title %></a></td>
                <td><%= diningPage.description %></td>
            </tr>
        <% }); %>
    </table>
</div>

<% include chunks/create_page %>
<% include chunks/foot %>